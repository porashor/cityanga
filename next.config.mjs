/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
    domains: [
      'media.istockphoto.com',
      'images.pexels.com',
      'cdn.pixabay.com',
    ]
  }
};

export default nextConfig;
